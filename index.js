const fs = require('fs');
const config = require('./config');
const graph = require('./graph_api');


console.log('Please make sure "graph_api_explorer_token" has been updated!');

// Check is there some data in `post_list.json`
//  YES => Next Step
//  NO => Get Feeds

// Ask the user whether to update `post_list.json` or not
//  YES => Update
//  NO => Get Feeds

// Get all information of posts in `post_list.json`

// Analysis